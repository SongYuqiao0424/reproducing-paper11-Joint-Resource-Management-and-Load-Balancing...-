本次将x<R的约束添加回来，同时对噪音和干扰项进行了修改：噪声项与波束选择进行耦合，当本次不选择开启时噪声也为0，干扰项则是进行泰勒展开，确保速率R永不为负。
从打印内容发现确实可以做到正确的波束激活，但是迭代过程中F不会存在4个1，X_var也显示无数值，需探究原因

多次仿真后发现本次仿真结果为特例，大部分情况下仍无法正常求解，打印速率R中相关系数后发现其量级过小，检查后发现信道模块生成的增益已经是|h|^2，但是在算法中当作|h|再次平方。