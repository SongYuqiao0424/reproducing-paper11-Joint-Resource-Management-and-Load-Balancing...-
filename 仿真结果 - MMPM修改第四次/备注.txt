本次修改回退了第三次半修改的干扰项泰勒展开内容，保留了对噪声项与波束模式的耦合（但感觉理论上不合理）；
修改了优化目标项，将主优化目标函数utility_surrogate的均衡系数(/L_0)修改为(/ np.max([1.0, np.max(Q_lengths)]))，让目标函数处于合理的数值范围;同时提高了惩罚项J的系数α、β、ρ的值确保F值接近0/1；
对求解器进行了修改，增加了最大迭代次数和收敛容差限制
对信道生成模块channel_model的返回值由|h|^2修正为|h|（否则是两次平方）

存在问题是虽然有约束波束激活总和<4，但是从打印结果发现大部分时候会超出限制，F=1的数量有时是5个，只是最后只采用了4个，与原文不一致